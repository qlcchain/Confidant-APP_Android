package com.stratagile.pnrouter.message

class MessageProvider {
    companion object {
        lateinit var instance: MessageProvider
        lateinit var messageList : HashMap<String, String>
    }

}